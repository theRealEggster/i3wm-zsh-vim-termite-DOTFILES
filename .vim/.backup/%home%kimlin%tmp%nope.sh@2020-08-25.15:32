#!/bin/bash

function LOG {
$* |& while read out; do
          echo "$(date '+[%y/%m/%d %H:%M:%S]')" "$out"
      done |& tee
}

#LOG echo "Press Enter to --- or exit script with ^C" && read -p "Enter / ^C: "

enter -n "namespace: "
read ns
echo $ns

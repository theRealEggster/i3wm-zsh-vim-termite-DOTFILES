#!/bin/sh
# shell script to prepend i3status with more stuff

i3status | while :
do
        read line
        TEMP=$(acpi -t |grep -o ,.* |sed -e s/,//g -e s/degrees//g -e 's/  //g')
        echo "Temp: $TEMP | $line" || exit 1
done

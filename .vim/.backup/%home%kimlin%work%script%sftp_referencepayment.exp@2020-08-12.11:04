#!/bin/expect -f

set timeout 10

spawn "bash for file in /home/kimlin/tmp/expectfile/; do echo $file; done"


#spawn bash -c "scp ~/tmp/expectfile/* root@IP:/tmp/"
#expect {
#  -re ".*es.*o.*" {
#    exp_send "yes\r"
#    exp_continue
#  }
#  -re ".*sword.*" {
#    exp_send "Password\r"
#  }
#}
#interact
